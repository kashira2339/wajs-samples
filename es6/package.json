{
  "scripts": {
    "start": "babel src -d dist --presets=latest -w",
    "build": "babel src -d dist --presets=latest"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-latest": "^6.16.0"
  }
}
