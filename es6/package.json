{
  "scripts": {
    "start": "babel src -d dist --presets=es2015 -w",
    "build": "babel src -d dist --presets=es2015"
  },
  "devDependencies": {
    "babel-cli": "^6.18.0",
    "babel-preset-es2015": "^6.18.0"
  }
}
